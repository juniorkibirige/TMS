function getCookie(cname) {
    var name = cname + "=";
    var decodedCookie = decodeURIComponent(document.cookie);
    var ca = decodedCookie.split(';');
    for (var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "";
}
function checkCookie() {
    var darkmode = getCookie("darkmode");
    return Boolean(darkmode);
}
!function () {
    const e = document,
        t = e.documentElement,
        n = e.body,
        i = e.getElementById("lights-toggle"),
        nb = e.getElementsByTagName('nav'),
        nb_btn = e.getElementById('nav-btn'),
        s = window.sr = ScrollReveal();
    window.darkmode = function () {
        let e = i.parentNode.querySelector(".label-text");
        i.checked
            ? (n.classList.remove("lights-off"),
                e && (e.innerHTML = "on"),
                (
                    window.localStorage.setItem("darkMode", false),
                    nb[0].classList.remove("navbar-inverse"),
                    nb[0].classList.add("navbar-default"),
                    nb_btn.classList.remove("btn-inverse"),
                    nb_btn.classList.add("btn-default")
                ))
            : (n.classList.add("lights-off"),
                e && (e.innerHTML = "off"),
                (
                    window.localStorage.setItem("darkMode", true),
                    nb[0].classList.remove("navbar-default"),
                    nb[0].classList.add("navbar-inverse"),
                    nb_btn.classList.remove("btn-default"),
                    nb_btn.classList.add("btn-inverse")
                )
            )
    };
    window.ret_no_dark = function () {
        let e = i.parentNode.querySelector(".label-text");
        n.classList.remove("lights-off"),
            e && (e.innerHTML = "on"),
            (
                i.checked = true,
                nb[0].classList.remove("navbar-inverse"),
                nb[0].classList.add("navbar-default"),
                nb_btn.classList.remove("btn-inverse"),
                nb_btn.classList.add("btn-default")
            )
    }
    window.ret_dark = function () {
        let e = i.parentNode.querySelector(".label-text");
        n.classList.add("lights-off"),
            e && (e.innerHTML = "off"),
            (
                i.checked = false,
                nb[0].classList.remove("navbar-default"),
                nb[0].classList.add("navbar-inverse"),
                nb_btn.classList.remove("btn-default"),
                nb_btn.classList.add("btn-inverse")
            )
    }
    t.classList.remove("no-js"),
        t.classList.add("js"),
        window.addEventListener("load",
            function () {
                n.classList.add("is-loaded")
            }),
        n.classList.contains("has-animations") && window.addEventListener("load", function () {
            s.reveal(".feature", {
                duration: 600,
                distance: "20px",
                easing: "cubic-bezier(0.215, 0.61, 0.355, 1)",
                origin: "right",
                viewFactor: .2
            })
        }),
        i && (i.addEventListener("change", window.darkmode))
}();